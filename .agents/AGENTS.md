# Project-Scoped Rules

- **Clear Error & Status Notifications**: Always implement clear, user-friendly, and visible notifications or status indicators (e.g. ErrorBoundaries, banner alerts, toast messages, helper text) whenever a task fails, a database/network error occurs, or a fallback mechanism is activated. Never let the system fail silently or show blank states.
